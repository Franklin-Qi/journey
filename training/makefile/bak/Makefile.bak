
%.o:%.c
	$(CC) -c $(CFLAGS) $< -o $@

# 自动化变量
# $@: 规则中的目标文件名
# $<: 规则中的第一个依赖文件
# $^: 规则中的所有依赖列表
# $*: 模式规则和静态模式中, $* 指代模式匹配符 % 匹配的部分， 比如% 匹配 f1.txt 中的f1 ，$* 就表示 f1。
cn.o:cn.c cn.h
	@echo $@
	@echo $<
	@echo $^
	@echo $*

